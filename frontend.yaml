---
- name: configure frontend server
  hosts: frontend
  become: yes
  tasks: 
  - name: install nginx
    ansible.builtin.dnf:
      name: nginx
      state: installed

  - name: enable and start the nginx service
    ansible.builtin.service:
      name: nginx
      enabled: yes
      state: started